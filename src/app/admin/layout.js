// src/app/admin/layout.js
// Admin sayfaları kişiseldir; statik üretilmez.
// (Faz 6: isAdmin kontrolü ve admin navigasyonu buraya eklenecek)
export const dynamic = "force-dynamic";

export default function AdminLayout({ children }) {
  return children;
}
